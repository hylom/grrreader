extends layout

block content
  #contents-body
    .row
      .span3
        ul.unstyled
          each feed in feeds
            li: a.feedItem(href='#', feed-id=feed.feed_id) #{feed.title}
      .span9
        table.table#contentsPane
